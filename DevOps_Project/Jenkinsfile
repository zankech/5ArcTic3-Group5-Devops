pipeline {
    agent any
    stages {
        stage('Check Maven Version') {
            steps {
                script {
                    env.JENKINS_CI = 'true'
                    sh 'mvn -v'
                }
            }
        }
    }
}
