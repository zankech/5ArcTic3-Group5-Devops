pipeline {
    agent any

    stages {
        stage('Check Maven Version') {
            steps {
                // Display the Maven version
                sh 'mvn -v'
            }
        }
    }

    post {
        always {
            cleanWs() // Clean workspace after the build
        }
    }
}
 
